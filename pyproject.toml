[tool.ruff]
line-length = 120
target-version = "py314"

select = [
  "E",   # pycodestyle errors
  "F",   # pyflakes
  "W",   # warnings
]

exclude = [
  ".venv",
  "venv",
  "__pycache__",
  "build",
  "dist",
]
